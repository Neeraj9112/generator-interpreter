{
  "name": "generator-interpreter",
  "version": "0.1.0",
  "description": "Pip — a small dynamic language whose evaluator is a generator",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc -p jsconfig.json",
    "web": "node web/serve.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
